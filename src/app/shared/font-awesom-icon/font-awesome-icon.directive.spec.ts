import { FontAwesomeIconDirective } from './font-awesome-icon.directive';

describe('FontAwesomeIconDirective', () => {
  it('should create an instance', () => {
    const directive = new FontAwesomeIconDirective();
    expect(directive).toBeTruthy();
  });
});
